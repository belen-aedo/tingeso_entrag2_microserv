spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tarifaespecialdb
    username: root
    password: nueva_clave
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
eureka:
  instance:
    preferIpAddress: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka